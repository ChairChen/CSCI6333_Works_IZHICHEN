export default function SettingsPage() {
  return (
    <div
      style={{
        padding: "2rem",
        borderRadius: "8px",
        width: "350px",
        border: "1px solid var(--color-text)"
      }}
    >
      <h2>Settings</h2>
      <p>This page is intentionally left blank as covered in class.</p>
    </div>
  );
}
